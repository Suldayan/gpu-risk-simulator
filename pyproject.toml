[project]
name = "gpu-risk-simulator"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["modules", "core"]


[build-system]
requires = ["setuptools>=82.0.1"]
build-backend = "setuptools.build_meta"